# watsonx Demo - Environment Configuration
# Copy this file to .env and fill in your credentials
#
# How to get credentials:
# - watsonx.ai: https://dataplatform.cloud.ibm.com/
# - IBM Cloud Object Storage: https://cloud.ibm.com/catalog/services/cloud-object-storage
# - watsonx.data: https://dataplatform.cloud.ibm.com/docs/content/wsj/wx-data/intro-wdp.html

# ============================================================================
# watsonx.ai Credentials (Required)
# ============================================================================
# Get your API key from: https://dataplatform.cloud.ibm.com/ -> User menu -> Access (API keys)
WATSONX_API_KEY=your_api_key_here

# Get your Project ID from: watsonx.ai project URL or settings
WATSONX_PROJECT_ID=your_project_id_here

# Space IDs: Development and Production environments
# Get from: watsonx.ai Space settings or URL
WATSONX_SPACE_ID_DEV=your_dev_space_id_here
WATSONX_SPACE_ID_PROD=your_prod_space_id_here

# watsonx.ai URL (usually https://us-south.ml.cloud.ibm.com or your region)
WATSONX_URL=https://us-south.ml.cloud.ibm.com

# ============================================================================
# Model Configuration (Required)
# ============================================================================
# Foundation model name (e.g., "meta-llama/llama-3-3-70b-instruct", "mistralai/mistral-small-3-1-24b-instruct-2503")
MODEL_NAME=meta-llama/llama-3-3-70b-instruct

# Embedding model ID (used for vector operations)
EMBEDDING_MODEL_ID=ibm/granite-embedding-107m-multilingual
# Alternative: EMBEDDING_MODEL (some scripts may use this)
EMBEDDING_MODEL=ibm/granite-embedding-107m-multilingual

# Model inference parameters
MAX_NEW_TOKENS=512
TEMPERATURE=0.7
TOP_P=0.9
TOP_K=50

# ============================================================================
# IBM Cloud Object Storage (COS) Credentials
# ============================================================================
# Required for: Vector index storage, document uploads (examples 04, 05+)
# Optional for: Basic model inference
# 
# Get credentials from: IBM Cloud -> Cloud Object Storage -> Service credentials
COS_BUCKET=your_cos_bucket_name_here
COS_CONNECTION_ASSET_ID=your_cos_connection_asset_id_here

# COS Access Credentials (if not using connection asset)
COS_ACCESS_KEY=your_cos_access_key
COS_SECRET_KEY=your_cos_secret_key
COS_ENDPOINT=s3.us-south.cloud-object-storage.appdomain.cloud

# Optional: Prefix for organizing files in COS bucket
COS_PREFIX=datasets

# ============================================================================
# Vector Index Configuration
# ============================================================================
# Required for: Vector search examples (05, 06, 07, 08, 11)
# These values are generated after running example 05_create_vector_index.py

# Vector index metadata
VECTOR_INDEX_NAME=documents_vector_index
VECTORIZED_DOCUMENT_ASSET_NAME=vectorized_document
VECTORIZED_DOCUMENT_ASSET_DESCRIPTION=Vector index for watsonx demo project

# Generated IDs (filled automatically after running examples)
# Uncomment and fill these after running example 05_create_vector_index.py
# VECTORIZED_DOCUMENT_ASSET_ID=your_vectorized_document_asset_id
# VECTOR_INDEX_ASSET_ID=your_vector_index_asset_id
# VECTOR_INDEX_ID_IN_PROJECT=your_vector_index_id_in_project

# ============================================================================
# watsonx.data / Milvus Connection (Optional)
# ============================================================================
# Required for: External vector store connections
# Get connection details from: watsonx.data instance -> Connections
# 
# Note: Variable names use MILVUS_ prefix for consistency with code

# Milvus Connection ID (from watsonx.data connection asset)
MILVUS_CONNECTION_ID=your_watsonx_data_milvus_connection_id_here

# Direct Milvus connection details (if not using connection asset)
# Get from: watsonx.data instance -> Connection details
MILVUS_HOST=your_milvus_host_here
MILVUS_PORT=31592
MILVUS_API_KEY=your_watsonx_data_api_key_here
MILVUS_DBNAME=default

# ============================================================================
# Document Processing Configuration
# ============================================================================
# Chunking parameters for document ingestion (example 06)
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Vector search parameters (example 07)
TOP_K=5

# ============================================================================
# Additional Configuration (Optional)
# ============================================================================
# Vector database type (default: 'milvus')
# VECTOR_DB_TYPE=milvus

# External vector index name (if using external Milvus)
# VECTOR_INDEX_NAME_EXTERNAL=documents_vector_index

# ============================================================================
# watsonx Orchestrate (Optional)
# ============================================================================
# Required for: watsonx Orchestrate examples (examples/wxo/*)
# Get credentials from: watsonx Orchestrate instance -> Settings -> API Keys
WXO_API_KEY=your_wxo_api_key_here
WXO_URL=https://api.us-south.watson-orchestrate.cloud.ibm.com/instances/your_instance_id_here

